
# Copy to .env and fill in values
# Doppler (read token for project/config below)
DOPPLER_TOKEN=dp.st.xxxxxxxxxxxxxxxxx

# Doppler location of your CF tunnel token
DOPPLER_PROJECT=trade
DOPPLER_CONFIG=prod

# Optional: if you don't want to use Doppler for cloudflared (set USE_DOPPLER_FOR_CLOUDFLARED=false)
# CLOUDFLARE_TUNNEL_TOKEN=

# Terraform toggles (can also be changed via -var flags)
# ENABLE_CLOUDFLARED=true
# USE_DOPPLER_FOR_CLOUDFLARED=true
# ENABLE_BOT=true
